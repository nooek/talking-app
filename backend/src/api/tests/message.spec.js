const request = require("supertest")
const app = require("../../../server")

describe('Test get message', () => {
    const BASE_URL = ''
    it('should get message', async (done) => {
        const response = request('')
        
        done()
    })
})